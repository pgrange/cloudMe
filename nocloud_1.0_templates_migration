#!/bin/bash

mkdir -p ~/.local/share/nocloud
[ -d ~/Documents/kvm ] && mv ~/Documents/kvm ~/.local/share/nocloud
for file in $(find ~/.local/share/nocloud -name "*.conf")
do
  echo $file
  sed -i -r 's:(vmDisk=).*/([^/]+)$:\1\2:' $file
  echo
done
